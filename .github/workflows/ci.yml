name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'screenshots/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'screenshots/**'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Hello
        run: echo "CI is wired up!"